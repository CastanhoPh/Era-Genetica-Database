# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Segredos: o vite.config.ts injeta variáveis de ambiente no bundle do cliente,
# então um .env aqui vai para o site publicado E para o histórico do git.
.env
.env.*
!.env.example
.firebase

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.claude
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Retrato estatico das colecoes publicas: gerado pelo `npm run dados` a cada deploy.
# Fica fora do git porque e derivado do Firestore e mudaria 1,6 MB a cada geracao.
/public/dados/
